using AutoMapper;
using TestMVC_2.Data;
using TestMVC_2.Models;

namespace TestMVC_2
{
    public class MappingProfile : Profile
    {
        public MappingProfile()
        {
            CreateMap<UserRegistrationModel, User>()
                .ForMember(u => u.UserName, opt => opt.MapFrom(x => x.Email));
        }
    }
}
